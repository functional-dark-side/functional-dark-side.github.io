# Site settings
title:
email: "dark@metagenomics.eu"
description: > # this means to ignore newlines until "baseurl:"
baseurl: "" # the subpath of your site, e.g. /blog/
url: "" # the base hostname & protocol for your site

twitter_username:
github_username: "functional-dark-side"

# Build settings
markdown: kramdown
collections:
  portfolio:
kramdown:
  parse_block_html: true

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: false
